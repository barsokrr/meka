# Railway gecici yayin — SQLite kalici disk ile tam calisir
# https://railway.app

[build]
builder = "nixpacks"
buildCommand = "npm run build:deploy"

[deploy]
startCommand = "npm run start:railway"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
